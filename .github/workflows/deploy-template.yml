name: Deploy Rules to SIEM
on:
  workflow_call:
    inputs:
      target:
        description: Target SIEM platform (elastic/splunk/sentinel)
        required: true
        type: string
  workflow_dispatch:
    inputs:
      target:
        description: Target SIEM platform
        required: true
        type: choice
        options:
          - elastic
          - splunk
          - sentinel

permissions:
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Elastic
        if: ${{ inputs.target == 'elastic' }}
        run: echo 'TODO POST elastic ndjson to Kibana saved_objects import API'

      - name: Deploy to Splunk
        if: ${{ inputs.target == 'splunk' }}
        run: echo 'TODO POST splunk conf to Splunk REST API'

      - name: Deploy to Sentinel
        if: ${{ inputs.target == 'sentinel' }}
        run: echo 'TODO PUT sentinel json to Azure Sentinel API'
