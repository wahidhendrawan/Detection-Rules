name: Integration Test (Elastic)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test-elastic:
    runs-on: ubuntu-latest
    services:
      elasticsearch:
        image: docker.elastic.co/elasticsearch/elasticsearch:8.13.0
        env:
          discovery.type: single-node
          xpack.security.enabled: false
        ports:
          - 9200:9200
    steps:
      - uses: actions/checkout@v4
      - name: Wait for Elasticsearch
        run: until curl -s http://localhost:9200; do sleep 5; done
      - name: Import rules (placeholder)
        run: echo 'TODO: import elastic/*.ndjson via saved_objects API'
      - name: Inject synthetic events (placeholder)
        run: echo 'TODO: inject test events matching rule conditions'
      - name: Verify alerts (placeholder)
        run: echo 'TODO: query .alerts-* index for expected matches'
